// Импортируется первой строкой скрипта: модули ESM выполняются в порядке зависимостей,
// а тело самого скрипта — последним. Загрузи .env в нём — и клиент базы, читающий
// окружение на старте, увидит пустоту.
import { existsSync } from 'node:fs'

if (existsSync('.env')) process.loadEnvFile('.env')
